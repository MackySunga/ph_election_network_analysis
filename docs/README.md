# Docs

This folder contains lightweight public-facing documentation assets.

`self_guided_presentation/` contains the self-guided HTML presentation app. It is small enough to keep in GitHub and can be served directly through GitHub Pages or any static host.

The heavy generated notebook outputs are not stored here. To regenerate analysis outputs, run the notebooks from the project root.
